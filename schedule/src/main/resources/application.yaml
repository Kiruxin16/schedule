server:
  port: 8191
  servlet:
    context-path: /schedule #/three-oceans.fitness

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/scheduledb
    username: postgres
    password: 123
  jpa:
    database: POSTGRESQL
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL10Dialect
